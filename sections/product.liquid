{% comment %}
  Modern Product Section
  Styled to match product-hero.liquid but uses actual product data
{% endcomment %}

{% render 'theme-styles-variables' %}

<div class="product" style="
  --section-padding-top: {{ section.settings.padding_top | default: 1 }}rem;
  --section-padding-bottom: {{ section.settings.padding_bottom | default: 1 }}rem;
  --section-padding-top-mobile: {{ section.settings.padding_top | default: 1 | times: 0.7 }}rem;
  --section-padding-bottom-mobile: {{ section.settings.padding_bottom | default: 1 | times: 0.7 }}rem;
  --section-padding-top-small: {{ section.settings.padding_top | default: 1 | times: 0.5 }}rem;
  --section-padding-bottom-small: {{ section.settings.padding_bottom | default: 1 | times: 0.5 }}rem;
">
  <script src="{{ 'product.js' | asset_url }}" defer="defer"></script>

  <div class="product__container">
    <div class="product__content">
      <div class="grid__item product__media-wrapper">
        {% render 'product-media-gallery', variant_images: product.images %}
      </div>
      <div class="product__info-wrapper grid__item">
        <section id="ProductInfo-{{ section.id }}" class="product__info-container">
          {%- assign product_form_id = 'product-form-' | append: section.id -%}

          {%- for block in section.blocks -%}
            {%- case block.type -%}
              {%- when '@app' -%}
                {% render block %}
              
              {%- when 'title' -%}
                <div class="product__title" {{ block.shopify_attributes }}>
                  <h1>{{ product.title | escape }}</h1>
                </div>
              
              {%- when 'price' -%}
                <div id="price-{{ section.id }}" class="price" {{ block.shopify_attributes }}>
                  <span class="price__regular">
                    {{ product.selected_or_first_available_variant.price | money }}
                  </span>
                  {%- if product.selected_or_first_available_variant.compare_at_price > product.selected_or_first_available_variant.price -%}
                    <span class="price__compare">
                      {{ product.selected_or_first_available_variant.compare_at_price | money }}
                    </span>
                  {%- endif -%}
                </div>
              
              {%- when 'description' -%}
                {%- if product.description != blank -%}
                  <div class="product__description rte" {{ block.shopify_attributes }}>
                    {{ product.description }}
                  </div>
                {%- endif -%}
              
              {%- when 'variant_picker' -%}
                {% render 'product-variant-picker', product: product, block: block, product_form_id: product_form_id %}
              
              {%- when 'quantity_selector' -%}
                <div class="product-form__input product-form__quantity" {{ block.shopify_attributes }}>
                  <label class="form__label" for="Quantity-{{ section.id }}">
                    {{ 'products.quantity' | t }}
                  </label>
                  <div class="quantity">
                    <button class="quantity__button" name="minus" type="button">
                      <span class="visually-hidden">{{ 'products.decrease_quantity' | t }}</span>
                      <span class="svg-wrapper">
                        {{- 'minus.svg' | inline_asset_content -}}
                      </span>
                    </button>
                    <input
                      class="quantity__input"
                      type="number"
                      name="quantity"
                      id="Quantity-{{ section.id }}"
                      min="1"
                      value="1"
                      form="{{ product_form_id }}"
                    >
                    <button class="quantity__button" name="plus" type="button">
                      <span class="visually-hidden">{{ 'products.increase_quantity' | t }}</span>
                      <span class="svg-wrapper">
                        {{- 'plus.svg' | inline_asset_content -}}
                      </span>
                    </button>
                  </div>
                </div>
              
              {%- when 'buy_buttons' -%}
                <form class="product-form__buttons" {{ block.shopify_attributes }}>
                  <button id="add-to-cart-button"
                    type="submit"
                    name="add"
                    class="button button--full-width button--secondary"
                    {% unless product.selected_or_first_available_variant.available %}disabled{% endunless %}
                    form="{{ product_form_id }}"
                  >
                    {%- if product.selected_or_first_available_variant.available -%}
                      {{ 'products.add_to_cart' | t }}
                    {%- else -%}
                      {{ 'products.sold_out' | t }}
                    {%- endif -%}
                  </button>
                </form>
              
              {%- when 'text' -%}
                <p class="product__text" {{ block.shopify_attributes }}>
                  {{- block.settings.text -}}
                </p>
              
              {%- when 'custom_code' -%}
                {{ block.settings.custom_code }}
              
              {%- when 'collapsible_tab' -%}
                <div class="product__accordion accordion" {{ block.shopify_attributes }}>
                  <details id="Details-{{ block.id }}-{{ section.id }}">
                    <summary>
                      <div class="summary__title">
                        <h2 class="h4 accordion__title">
                          {{ block.settings.heading | default: block.settings.page.title | escape }}
                        </h2>
                      </div>
                      <span class="svg-wrapper">
                        {{- 'chevron-down.svg' | inline_asset_content -}}
                      </span>
                    </summary>
                    <div class="accordion__content rte">
                      {{ block.settings.content }}
                      {{ block.settings.page.content }}
                    </div>
                  </details>
                </div>
                
              {%- when 'rating' -%}
                <div class="product__rating" {{ block.shopify_attributes }}>
                  <div class="rating-stars">
                    {% assign rating = block.settings.rating %}
                    {% assign full_stars = rating | floor %}
                    {% assign has_half = rating | modulo: 1 %}
                    
                    {% for i in (1..5) %}
                      {% if i <= full_stars %}
                        <span class="star star--full">★</span>
                      {% else %}
                        {% assign next_star = full_stars | plus: 1 %}
                        {% if i == next_star and has_half > 0 %}
                          <span class="star star--half">★</span>
                        {% else %}
                          <span class="star star--empty">☆</span>
                        {% endif %}
                      {% endif %}
                    {% endfor %}
                  </div>
                  <span class="rating-value">{{ rating }}</span>
                  <span class="rating-text">{{ block.settings.review_count }}</span>
                </div>
                
              {% when 'features' %}
                {% if block.settings.features != blank %}
                  <div class="product__features" {{ block.shopify_attributes }}>
                    {% if block.settings.heading != blank %}
                      <h3 class="features__heading">{{ block.settings.heading }}</h3>
                    {% endif %}
                    <ul class="features__list">
                      {% assign features_array = block.settings.features | split: ',' %}
                      {% for feature in features_array %}
                        {% if feature != blank %}
                          <li class="feature__item">
                            <span class="feature__icon">✓</span>
                            {{ feature | strip }}
                          </li>
                        {% endif %}
                      {% endfor %}
                    </ul>
                  </div>
                {% endif %}
                
              {%- when 'customer_satisfaction' -%}
                <div class="product__customer-satisfaction" {{ block.shopify_attributes }}>
                  <div class="customer-avatars">
                    {% if block.settings.avatar_1 %}
                      <img src="{{ block.settings.avatar_1 | image_url: width: 80, height: 80 }}" 
                           alt="Customer" class="customer-avatar" width="80" height="80">
                    {% endif %}
                    {% if block.settings.avatar_2 %}
                      <img src="{{ block.settings.avatar_2 | image_url: width: 80, height: 80 }}" 
                           alt="Customer" class="customer-avatar" width="80" height="80">
                    {% endif %}
                    {% if block.settings.avatar_3 %}
                      <img src="{{ block.settings.avatar_3 | image_url: width: 80, height: 80 }}" 
                           alt="Customer" class="customer-avatar" width="80" height="80">
                    {% endif %}
                    {% if block.settings.avatar_4 %}
                      <img src="{{ block.settings.avatar_4 | image_url: width: 80, height: 80 }}" 
                           alt="Customer" class="customer-avatar" width="80" height="80">
                    {% endif %}
                  </div>
                  <span class="product__customers">{{ block.settings.customers_text }}</span>
                  <div class="product__stock">
                    <span class="product__stock-dot"></span>
                    {{ block.settings.stock_text }}
                  </div>  
                </div>
                  
                
              {%- when 'usp_icons' -%}
                <div class="product__usp-icons" {{ block.shopify_attributes }}>
                  <div class="usp-grid">
                    {% if block.settings.text_1 != blank %}
                      <div class="usp-item">
                        <div class="usp-icon">
                          {{- block.settings.icon_1_name | append: '.svg' | inline_asset_content -}}
                        </div>
                        <span class="usp-text">{{ block.settings.text_1 }}</span>
                      </div>
                    {% endif %}
                    
                    {% if block.settings.text_2 != blank %}
                      <div class="usp-item">
                        <div class="usp-icon">
                          {{- block.settings.icon_2_name | append: '.svg' | inline_asset_content -}}
                        </div>
                        <span class="usp-text">{{ block.settings.text_2 }}</span>
                      </div>
                    {% endif %}
                    
                    {% if block.settings.text_3 != blank %}
                      <div class="usp-item">
                        <div class="usp-icon">
                          {{- block.settings.icon_3_name | append: '.svg' | inline_asset_content -}}
                        </div>
                        <span class="usp-text">{{ block.settings.text_3 }}</span>
                      </div>
                    {% endif %}
                    
                    {% if block.settings.text_4 != blank %}
                      <div class="usp-item">
                        <div class="usp-icon">
                          {{- block.settings.icon_4_name | append: '.svg' | inline_asset_content -}}
                        </div>
                        <span class="usp-text">{{ block.settings.text_4 }}</span>
                      </div>
                    {% endif %}
                  </div>
                </div>
               
            {%- endcase -%}
          {%- endfor -%}
          
          <!-- Product Form -->
          {%- form 'product', product, id: product_form_id, class: 'product-form' -%}
            <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}">
            
            <!-- Quantity selector for Kaching Bundles compatibility -->
            <div class="product-form__quantity-wrapper">
              <label class="form__label" for="Quantity-{{ product_form_id }}">
                {{ 'products.quantity' | t }}
              </label>
              <div class="quantity">
                <button class="quantity__button" name="minus" type="button">
                  <span class="visually-hidden">{{ 'products.decrease_quantity' | t }}</span>
                  <span class="svg-wrapper">
                    {{- 'minus.svg' | inline_asset_content -}}
                  </span>
                </button>
                <input
                  class="quantity__input"
                  type="number"
                  name="quantity"
                  id="Quantity-{{ product_form_id }}"
                  min="1"
                  value="1"
                >
                <button class="quantity__button" name="plus" type="button">
                  <span class="visually-hidden">{{ 'products.increase_quantity' | t }}</span>
                  <span class="svg-wrapper">
                    {{- 'plus.svg' | inline_asset_content -}}
                  </span>
                </button>
              </div>
            </div>
          {%- endform -%}
          
        </section>
      </div>
    </div>
  </div>

  {% assign variant_images = product.images %}
  {% render 'product-media-modal', variant_images: variant_images %}

  {%- liquid
    if product.selected_or_first_available_variant.featured_media
      assign seo_media = product.selected_or_first_available_variant.featured_media
    else
      assign seo_media = product.featured_media
    endif
  -%}

  <script type="application/ld+json">
    {{ product | structured_data }}
  </script>
</div>

{% stylesheet %}
/* Product Section Layout */
.product {
  /* Ensure proper section layout */
  width: 100%;
  max-width: 100%;
  overflow-x: hidden;
  padding-top: var(--section-padding-top);
  padding-bottom: var(--section-padding-bottom);
}

@media screen and (max-width: 749px) {
  .product {
    padding-top: var(--section-padding-top-mobile);
    padding-bottom: var(--section-padding-bottom-mobile);
  }
}

@media screen and (max-width: 480px) {
  .product {
    padding-top: var(--section-padding-top-small);
    padding-bottom: var(--section-padding-bottom-small);
  }
}

/* Product Container */
.product__container {
  max-width: var(--container-max-width);
  margin: 0 auto;
  padding: 0 var(--spacing-sm);
  width: 100%;
  box-sizing: border-box;
  
  /* Follow Shopify's section grid system */
  position: relative;
  z-index: 1;
}

/* Product Content */
.product__content {
  display: grid;
  grid-template-columns: 1fr;
  gap: var(--spacing-xl);
  align-items: start;
  min-height: 600px;
  width: 100%;
  box-sizing: border-box;
}

@media screen and (min-width: 750px) {
  .product__content {
    grid-template-columns: 1fr 1fr;
  }
}



/* Media Gallery */
.product__media-wrapper {
  position: relative;
}

/* Product Info */
.product__info-wrapper {
  padding: var(--spacing-md);
  background: var(--color-bg-primary);
  box-sizing: border-box;
}

.product__info-container {
  display: flex;
  flex-direction: column;
}

/* Block Styles */
.product__title h1 {
  font-size: var(--font-size-xl);
  font-weight: var(--font-weight-bold);
  line-height: var(--line-height-tight);
  color: var(--color-text-primary);
  margin: 0 0 var(--spacing-lg) 0;
}

.price {
  display: flex;
  align-items: center;
  gap: var(--spacing-md);
  font-size: var(--font-size-xl);
  margin-bottom: var(--spacing-md);
}

.price__compare {
  text-decoration: line-through;
  color: var(--color-text-muted);
}

.product__description {
  font-size: var(--font-size-lg);
  color: var(--color-text-secondary);
  margin: 0 0 var(--spacing-lg) 0;
  line-height: var(--line-height-normal);
}

.product-form__input {
  margin-bottom: var(--spacing-md);
}

.form__label {
  display: block;
  margin-bottom: var(--spacing-sm);
  font-weight: var(--font-weight-medium);
  color: var(--color-text-primary);
}

.quantity {
  display: flex;
  align-items: center;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  overflow: hidden;
}

.quantity__button {
  background: var(--color-bg-light);
  border: none;
  padding: var(--spacing-sm);
  cursor: pointer;
  transition: background-color var(--transition-fast);
}

.quantity__button:hover {
  background: var(--color-border);
}

.quantity__input {
  border: none;
  text-align: center;
  padding: var(--spacing-sm);
  width: 4rem;
  outline: none;
}

.product-form__buttons {
  margin-top: var(--spacing-md);
}

.button {
  display: inline-block;
  padding: var(--spacing-lg) var(--spacing-xl);
  border: none;
  border-radius: var(--radius-md);
  text-decoration: none;
  text-align: center;
  cursor: pointer;
  transition: all var(--transition-fast);
  font-weight: var(--font-weight-semibold);
  font-size: var(--font-size-base);
}

.button--full-width {
  width: 100%;
}

.button--secondary {
  background: var(--color-text-primary);
  color: var(--color-text-light);
}

.button--secondary:hover {
  background: #333333;
}

.button:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

/* Text and Custom Liquid Blocks */
.product__text {
  margin: 0 0 var(--spacing-md) 0;
  line-height: var(--line-height-relaxed);
  color: var(--color-text-secondary);
}

/* Accordion/Collapsible Tab Styles */
.product__accordion {
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  margin-bottom: var(--spacing-md);
  overflow: hidden;
}

.accordion__title {
  margin: 0;
  font-size: var(--font-size-lg);
  font-weight: var(--font-weight-semibold);
  color: var(--color-text-primary);
}

.accordion__content {
  padding: var(--spacing-md);
  border-top: 1px solid var(--color-border);
  background: var(--color-bg-light);
}

.svg-wrapper {
  display: inline-flex;
  align-items: center;
  justify-content: center;
}

.svg-wrapper svg {
  width: 1rem;
  height: 1rem;
  color: var(--color-text-secondary);
}

/* Rating Block */
.product__rating {
  display: flex;
  align-items: center;
  gap: var(--spacing-sm);
  margin-bottom: var(--spacing-lg);
}

.rating-stars {
  display: flex;
  gap: var(--spacing-xs);
}

.star {
  font-size: var(--font-size-lg);
  line-height: 1;
}

.star--full {
  color: #ffd700;
}

.star--half {
  color: #ffd700;
  opacity: 0.7;
}

.star--empty {
  color: var(--color-border);
}

.rating-value {
  font-weight: var(--font-weight-semibold);
  color: var(--color-text-primary);
}

.rating-text {
  color: var(--color-text-secondary);
  font-size: var(--font-size-sm);
}

.features__heading {
  font-size: var(--font-size-lg);
  font-weight: var(--font-weight-semibold);
  margin: 0 0 var(--spacing-md) 0;
  color: var(--color-text-primary);
}

.features__list {
  list-style: none;
  padding: 0;
  margin: 0 0 var(--spacing-lg) 0;
}

.feature__item {
  display: flex;
  align-items: flex-start;
  gap: var(--spacing-sm);
  margin-bottom: var(--spacing-sm);
  color: var(--color-text-primary);
}

.feature__icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 1.25rem;
  height: 1.25rem;
  background: var(--color-primary);
  color: var(--color-text-light);
  border-radius: var(--radius-full);
  font-size: var(--font-size-xs);
  font-weight: var(--font-weight-semibold);
  flex-shrink: 0;
}

/* Customer Satisfaction Block */
.product__customer-satisfaction {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  justify-content: center;
  gap: var(--spacing-sm);
  margin-bottom: var(--spacing-lg);
}

.customer-satisfaction__text {
  margin: 0 0 var(--spacing-sm) 0;
  font-size: var(--font-size-sm);
  color: var(--color-text-primary);
  font-weight: var(--font-weight-medium);
}

.product__customers {
  color: var(--color-text-secondary);
  font-size: var(--font-size-sm);
}

.product__stock {
  display: flex;
  align-items: center;
  gap: var(--spacing-xs);
  color: var(--color-success);
  font-size: var(--font-size-sm);
}

.product__stock-dot {
  width: 0.5rem;
  height: 0.5rem;
  background: var(--color-success);
  border-radius: var(--radius-full);
}

.customer-avatars {
  display: flex;
  gap: var(--spacing-xs);
}

.customer-avatar {
  width: 1.5rem;
  height: 1.5rem;
  border-radius: var(--radius-full);
  background: var(--color-primary);
  border: 0.125rem solid var(--color-bg-primary);
  object-fit: cover;
  object-position: center;
}

/* USP Icons Block */
.product__usp-icons {
  margin-bottom: var(--spacing-lg);
}

.usp-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: var(--spacing-sm);
}

.usp-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  gap: var(--spacing-sm);
}

.usp-icon {
  width: 2rem;
  height: 2rem;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--color-border);
  border-radius: var(--radius-md);
}

.usp-icon svg {
  width: 1.5rem;
  height: 1.5rem;
  color: var(--color-text-primary);
}

.usp-text {
  font-size: var(--font-size-xs);
  font-weight: var(--font-weight-semibold);
  color: var(--color-text-primary);
  text-align: center;
  line-height: var(--line-height-tight);
}

/* Button loading state */
.button:disabled {
  opacity: 0.6;
  cursor: not-allowed;
  transition: opacity var(--transition-fast);
}

/* Product Form Quantity Wrapper */
.product-form__quantity-wrapper {
  margin-bottom: var(--spacing-md);
  padding: var(--spacing-md);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  background: var(--color-bg-light);
}

.product-form__quantity-wrapper .form__label {
  display: block;
  margin-bottom: var(--spacing-sm);
  font-weight: var(--font-weight-medium);
  color: var(--color-text-primary);
}

.product-form__quantity-wrapper .quantity {
  display: flex;
  align-items: center;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  overflow: hidden;
  background: var(--color-bg-primary);
}

.product-form__quantity-wrapper .quantity__button {
  background: var(--color-bg-light);
  border: none;
  padding: var(--spacing-sm);
  cursor: pointer;
  transition: background-color var(--transition-fast);
}

.product-form__quantity-wrapper .quantity__button:hover {
  background: var(--color-border);
}

.product-form__quantity-wrapper .quantity__input {
  border: none;
  text-align: center;
  padding: var(--spacing-sm);
  width: 4rem;
  outline: none;
  background: var(--color-bg-primary);
}

/* Quantity info styling */
.product-form__quantity-info {
  margin-bottom: var(--spacing-md);
  padding: var(--spacing-md);
  background: var(--color-bg-light);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
}

.quantity-info__text {
  margin: 0;
  font-size: var(--font-size-sm);
  color: var(--color-text-secondary);
  text-align: center;
  font-style: italic;
}

/* Hide the quantity selector while keeping it functional for Kaching Bundles */
.product-form__quantity-wrapper {
  display: none !important;
}
{% endstylesheet %}

{% schema %}
{
  "name": "Product",
  "tag": "section",
  "class": "section",
  "blocks": [
    {
      "type": "title",
      "name": "Product Title",
      "limit": 1
    },
    {
      "type": "price",
      "name": "Price",
      "limit": 1
    },
    {
      "type": "description",
      "name": "Description",
      "limit": 1
    },
    {
      "type": "variant_picker",
      "name": "Variant Picker",
      "limit": 1,
      "settings": [
        {
          "type": "select",
          "id": "picker_type",
          "options": [
            {
              "value": "dropdown",
              "label": "{{ 'schema.dropdown' | t }}"
            },
            {
              "value": "button",
              "label": "{{ 'schema.button' | t }}"
            }
          ],
          "default": "dropdown",
          "label": "{{ 'schema.picker_type' | t }}"
        }
      ]
    },
    {
      "type": "quantity_selector",
      "name": "Quantity Selector",
      "limit": 1
    },
    {
      "type": "buy_buttons",
      "name": "Add to Cart",
      "limit": 1
    },
    {
      "type": "text",
      "name": "{{ 'schema.text' | t }}",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "default": "Add your text here",
          "label": "{{ 'schema.text' | t }}"
        }
      ]
    },
    {
      "type": "custom_code",
      "name": "Custom Code",
      "settings": [
        {
          "type": "liquid",
          "id": "custom_code",
          "label": "Custom Code",
          "info": "Add custom Liquid code here"
        }
      ]
    },
    {
      "type": "collapsible_tab",
      "name": "Collapsible Tab",
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "default": "Tab Heading",
          "label": "{{ 'schema.heading' | t }}"
        },
        {
          "type": "richtext",
          "id": "content",
          "label": "{{ 'schema.content' | t }}"
        },
        {
          "type": "page",
          "id": "page",
          "label": "{{ 'schema.page' | t }}"
        }
      ]
    },
    {
      "type": "rating",
      "name": "Rating & Reviews",
      "settings": [
        {
          "type": "range",
          "id": "rating",
          "min": 0,
          "max": 5,
          "step": 0.1,
          "default": 4.7,
          "label": "{{ 'schema.rating' | t }}"
        },
        {
          "type": "text",
          "id": "review_count",
          "default": "1000+ Verified Reviews",
          "label": "{{ 'schema.review_count_text' | t }}"
        }
      ]
    },
    {
      "type": "features",
      "name": "Features List",
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "default": "Key Features",
          "label": "{{ 'schema.heading' | t }}"
        },
        {
          "type": "textarea",
          "id": "features",
          "default": "Prevents risk from microplastics\nResists bacteria and odors\nKeeps your knives sharp\nEliminates food cross-contamination",
          "label": "{{ 'schema.features' | t }}",
          "info": "Enter each feature on a new line"
        }
      ]
    },
    {
      "type": "customer_satisfaction",
      "name": "Customer Satisfaction",
      "settings": [
        {
          "type": "text",
          "id": "customers_text",
          "label": "{{ 'schema.customers_text' | t }}",
          "default": "8K+ Happy Customers"
        },
        {
          "type": "text",
          "id": "stock_text",
          "label": "{{ 'schema.stock_text' | t }}",
          "default": "In stock and ready to ship"
        },
        {
          "type": "image_picker",
          "id": "avatar_1",
          "label": "{{ 'schema.customer_avatar' | t }} 1"
        },
        {
          "type": "image_picker",
          "id": "avatar_2",
          "label": "{{ 'schema.customer_avatar' | t }} 2"
        },
        {
          "type": "image_picker",
          "id": "avatar_3",
          "label": "{{ 'schema.customer_avatar' | t }} 3"
        },
        {
          "type": "image_picker",
          "id": "avatar_4",
          "label": "{{ 'schema.customer_avatar' | t }} 4"
        },
        {
          "type": "image_picker",
          "id": "avatar_5",
          "label": "{{ 'schema.customer_avatar' | t }} 5"
        }
      ]
    },
    {
      "type": "usp_icons",
      "name": "USP Icons",
      "settings": [
        {
          "type": "header",
          "content": "USP 1"
        },
        {
          "type": "text",
          "id": "icon_1_name",
          "default": "diamond",
          "label": "{{ 'schema.icon_name' | t }} 1"
        },
        {
          "type": "text",
          "id": "text_1",
          "default": "PURE TITANIUM",
          "label": "{{ 'schema.text_label' | t }} 1"
        },
        {
          "type": "header",
          "content": "USP 2"
        },
        {
          "type": "text",
          "id": "icon_2_name",
          "default": "clock",
          "label": "{{ 'schema.icon_name' | t }} 2"
        },
        {
          "type": "text",
          "id": "text_2",
          "default": "LIFETIME DURABILITY",
          "label": "{{ 'schema.text_label' | t }} 2"
        },
        {
          "type": "header",
          "content": "USP 3"
        },
        {
          "type": "text",
          "id": "icon_3_name",
          "default": "dollar-sign",
          "label": "{{ 'schema.icon_name' | t }} 3"
        },
        {
          "type": "text",
          "id": "text_3",
          "default": "30-DAY MONEY-BACK GUARANTEE",
          "label": "{{ 'schema.text_label' | t }} 3"
        },
        {
          "type": "header",
          "content": "USP 4"
        },
        {
          "type": "text",
          "id": "icon_4_name",
          "default": "shield",
          "label": "{{ 'schema.icon_name' | t }} 4"
        },
        {
          "type": "text",
          "id": "text_4",
          "default": "ANTIBACTERIAL AND BPA-FREE",
          "label": "{{ 'schema.text_label' | t }} 4"
        }
      ]
    }
  ],
  "settings": [
    {
      "type": "select",
      "id": "color_scheme",
      "options": [
        {
          "value": "accent-1",
          "label": "{{ 'schema.accent_1' | t }}"
        },
        {
          "value": "accent-2",
          "label": "{{ 'schema.accent_2' | t }}"
        },
        {
          "value": "background-1",
          "label": "{{ 'schema.background_1' | t }}"
        },
        {
          "value": "background-2",
          "label": "{{ 'schema.background_2' | t }}"
        }
      ],
      "default": "background-1",
              "label": "{{ 'schema.color_scheme' | t }}"
    },
    {
      "type": "select",
      "id": "media_size",
      "options": [
        {
          "value": "small",
          "label": "{{ 'schema.small' | t }}"
        },
        {
          "value": "medium",
          "label": "{{ 'schema.medium' | t }}"
        },
        {
          "value": "large",
          "label": "{{ 'schema.large' | t }}"
        }
      ],
      "default": "large",
              "label": "{{ 'schema.media_size' | t }}"
    },
    {
      "type": "select",
      "id": "media_position",
      "options": [
        {
          "value": "left",
          "label": "{{ 'schema.left' | t }}"
        },
        {
          "value": "right",
          "label": "{{ 'schema.right' | t }}"
        }
      ],
      "default": "left",
              "label": "{{ 'schema.media_position' | t }}"
    },
    {
      "type": "header",
      "content": "Padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 10,
      "step": 0.5,
      "unit": "rem",
                      "label": "{{ 'schema.padding_top' | t }}",
        "default": 2
      },
      {
        "type": "range",
        "id": "padding_bottom",
        "min": 0,
        "max": 10,
        "step": 0.5,
        "unit": "rem",
        "label": "{{ 'schema.padding_bottom' | t }}",
        "default": 2
    }
  ]
}
{% endschema %}


